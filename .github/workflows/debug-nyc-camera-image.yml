name: Debug NYC Camera Image

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Probe camera image URLs
        run: node debug-nyc-camera-image.js
