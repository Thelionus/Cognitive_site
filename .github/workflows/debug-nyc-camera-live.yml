name: Debug NYC Camera Live

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Probe live page and camera image
        run: node debug-nyc-camera-live.js
